# 34. 권한 관리 (/permissions)
- 권한 관리
- claude code
```text
개발자 웹 이력서를 개발할 수 있도록 ROADMAP.md 파일을 작성해주세요.
기술 스택:
- html, css, javascript, tailwindcss
이력서 내용:
- 일반적인 내용으로 간단히 작성해줘.
```
- 명령어
  - claude --continue
    - 이전 세션 대화내용을 이어나갈 수 있다.
    - 터미널 닫은 경우
  - resume
    - 이전 대화내역 확인 가능
```text
/init 프로젝트 초기화를 한국어로 진행해주세요.
다음 설정을 CLAUDE.md에 포함해주세요:

## 언어 및 커뮤니케이션 규칙
- ***기본 응답 및 언어**: 한국어
- **코드 주석**: 한국어로 작성
- **커밋 메시지**: 한국어로 작성
- **문서화**: 한국어로 작성
- **변수명/함수명**: 영어 (코드 표준 준수)
```
- 분석 -> 계획 -> 실행 워크 플로우
- claude code
  - /permissions
    - 현재 수락된 권한이 나온다.
      - bash
      - add rule -> Bash(npm run dev)
      - add rule -> Bash
        - scope
          - local
    - settings.local.json 파일에 해당 코드가 추가 된다.
    - 공식 문서 확인
    - workspace
      - 다른 프로젝트의 코드베이스를 참고하여 개발 진행 가능

# 35. 권한 모드 (accept, plan): 프로필 웹 사이트 만들기
- 중요!
- 클로두 코드가 기본적으로 어떻게 행동할 지 결정!
- 4가지 모드 존재
  - default: 표준 동작 - 첫 사용시 권한 요청
  - acceptEdits: 세션에 대해 파일 편집 권한을 자동으로 수락, 다른건 여전히 물어보라는 것 -> 개발 시 편하다! (여러 파일 수정, 생성, ...)
  - plan: 계획 모드 -> 쓰기 작업을 하진 않고, 분석만 해서 계획만 해준다. (중요)
  - interactive: -> 모든 권한 프롬프트를 건너뛴다.
- mode 변경
  - shift tab
  - plan mode on -> @ROADMAP.md 파일을 참고해서 개발해줘.
  - + plan mode -> index.html 에 SNS 링크를 포함하는 섹션을 추가해줘 - github, youtube, instargram, etc 
- 작업해줘를 하기 전 많은 plan mode를 활용한다.
- claude code -> 이미지 첨부
  - cursor는 클립보드에 복사된 이미지가 붙여지는데, 인텔리제이 터미널 안에서는 동작이 안 한다.
  - 이미지 첨부 후 수정 사항 -> plan mode on
    - 수정사항: \ - 웹의 전체적인 컨테이너 폭을 줄여주세요 \ - 안 보이는 이미지는 웹에서 샘플이미지를 사용해서 보이도록 해줘!

# 36. 클로드 코드 이미지 첨부 UI 변경 안내

# 37. 대화형 질문 기능 (Interactive Question Tool)
- plan mode -> 질문 -> 클로는 추가 정보를 묻는다. 

# 38. 향상된 플랜 모드: 계획 파일관리 및 clear 실행
- plan mode on
  - 옵션 외 입력 항목 추가됌 -> 플랜 재수정(~~ 수정해줘)
  - clear context and auto accept edits 
    - context 용량은 정해져 있다[context window]
      - 대화 내용이 context를 가득 차면 토큰 소비도 늘어나고 제대로된 답변을 못 얻을 수 있다.
    - 본격 작업 전, Context가 가득차 있으면 clear 후 자동 진행
    - 과거에는 context를 메모리로 저장해서 clear하면 안 됐지만, 현재(26-02-22)는 파일로 저장한다.
  - 플랜 모드 작성 중 esc
    - 다시 계획을 보고 싶다면 -> "/plan open" 명령어 입력
  - settings.json
      - { "plansDirectory" : ".claude/plans" } -> 플랜 파일 디렉토리 커스텀 지정 가능, 기존에는 특정 폴더에 들어가 있었는데, 프로젝트 안에서 관리 가능
      ```json
    
      ```

# 39. Extended Thinking: 확장된 사고
- 확장된 사고
  - 클로드가 응답 전, 내부적으로 추론 과정을 거치는 기능
    - 프롬프트 요청 -> 바로 답변 X -> 깊게 생각 후 답변
  - 기본적으로 활성화 되어 있다. -> 프롬프트로 활성화하는 기능 존재
    - 과거 -> 질문 후 ultrathink
      - ultrathink를 붙여야 확장 사고 모드 활성화
    - 토큰 부족시 확장 사고 비활성화 필요
      - /config
        - Thinking mode = true
          - entry로 false 처리로 변경하면 된다.
      - 단축키 변경
        - Option + T -> 키 맵핑이 겹치면 안 되는 경우도 있음 (설정 필요)

# 40. 확장 사고 (ultrathink) 및 이미지로 UI 개선
- 클로드 코드 사고 폭을 넓혀 해결하기
  - 프롬프트 -> 'think' 단어 사용 -> 확장 사고 활성화
    - think -> 4,000 tokens
    - think hard
    - megathink -> 10,000 tokens
    - ultrathink -> maximum budget -> 최대 예산 투자하여 사고, 복잡한 아키텍처, 성능 최적화, 전체적인 코드베이스 분석 -> 최고품질 요구시 이용
- 포트폴리오 디자인 요구
  - plan mode on
  ```text
  전체적인 디자인을 모던하고 세련된 디자인으로 해주세요.
  - 다크모드
  - 전체적인 레이아웃 및 간격 일관성
  - 모던하고 세련된 스타일 
  ultrathink
  ```
- 참고 사이트 -> 클로드에게 시각적인 자료 (이미지, ...)
  - [https://dribbble.com](드리블)
- 26.01.16 -> ultrathink 키워드 사용 X, 깊은 사고 모드 항상 on 으로 변경됌
- 사고 토큰
  - 클로드 코드 -> 고민 하는 시간
    - 사고 토큰 많음 -> 더 오래 깊이 생각하고 정확한 결과
    - 사고 토크 적음 -> 빠르게 생각 -> 빠른 응답, 비용 절약
    - 기본 값 31,999 토큰 -> 대부분 작업이 기본 값이면 충분
  - 사고 토큰 설정 변경
    - settings.json
# 41. ultrathink 키워드 변경 안내 (Extended Thinking)

# 42. 토큰 효과적으로 사용하기
- 프롬프트 토큰 절약
  - 프로필 웹 사이트 만들어줘 -> 과도한 생성으로 토큰 많이 사용
    - 복잡한 HTML 코드
    - 여러 개 분리된 파일
    - 배포 환경 설정 포함
    - 불필요한 최적화 및 테스트 코드
  - 토큰 절약 방법
    ```text
    간단한 프로필 웹사이트를 만들어줘:
    - 단일 HTML 파일로만 작성
    - 이름, 소개, SNS 링크만 포함
    - 외부 라이브러리 ㅏㅅ용하지 말고 순수 HTML/CSS만 사용
    - 배포, 테스트, 최적화 단계는 제외
    - 총 300줄 이하로 작성
    ```
    - 핵심 키워드
      - 간단한, 최소한 기능만
      - 단일 파일
      - 배포/테스트/최적화 제외
      - ~줄 이하로 작성
- model -> Haiku 사용
- 필수 명령어
  - /context -> 현재 컨텍스트에 포함된 파일 확인
  - /clear -> 불필요한 컨텍스트 제거
  - /compact -> 컨텍스트 압축으로 토큰 절약
    - /compact "회원 관련 ~~ 요약해줘.[요약 정보]"
    

# 43. 🔥[MISSION 5] 미션 소개🔥
- claude code
  ```text
  나만의 프로필 웹사이트를 만들고 싶어. 다음 정보를 포함해줘:
  - 이름: [본인 이름]
  - 직업/역할: [예: 프론트엔드 개발자, 학생 등]
  - 소개: [간단한 자기소개 2-3줄]
  - 기술 스택: [예: HTML, CSS, JavaScript]
  - 프로젝트/포트폴리오 섹션
  - 연락처: [이메일이나 GitHub 링크]

  HTML, CSS, JavaScript로 만들고 Tailwind CSS CDN을 사용해줘.
  모던하고 깔끔한 디자인으로 만들어줘.
  
  먼저 어떻게 만들 건지 계획을 보여줘.
  ```
  ```text
  계획은 좋은데, 다음을 수정해줘:
  - 색상 테마를 [파란색/보라색/녹색] 계열로 바꿔줘
  - 프로젝트 섹션을 카드 형태로 3개 배치해줘
  - 헤더에 네비게이션 메뉴를 추가해줘
  ```
  ```text
  좋아. 이 계획대로 구현해줘.
  ```
  ```text
  ❯ [Image #2][Image #3][Image #4]

  이 스크린샷들을 보고 다음 부분 수정해줘.
  1. #2 -> 맨 위 헤더 영역 - 폰트 크기를 더 크게
  2. #3 -> 가운데 소개 섹션 - 배경색을 조금 더 밝게
  3. #4 -> 하단 연락처 버튼 - 간격을 더 넓게
  ```
  ```text
  거의 완벽해! 이 스크린샷에서 마지막으로 다음을 조정해줘:
  1. #2 -> 상단의 이 네비게이션 메뉴 - 위치를 오른쪽으로
  2. #3 -> 중간의 이 프로젝트 카드들 - 호버 효과 추가
  3. #4 -> 하단의 이 푸터 영역 - 여백을 더 넓게
  ```

# 🔥[MISSION 5] 클로드 코드로 나만의 프로필 웹사이트 만들기🔥
