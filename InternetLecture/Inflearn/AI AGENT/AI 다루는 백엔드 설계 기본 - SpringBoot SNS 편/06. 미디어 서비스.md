# 39. 미디어 도메인 (오브젝트 스토리지, S3, RustFS)

---

# 40. 미디어 도메인 ▶︎ 구현과 동작

---

# 41. 미디어 업로드 (네트워크 부하)
- 업로드 방식
  - 백엔드 프록시
    - 클라이언트 -> 백엔드 -> S3 업로드
  - Pre-signed URL
    - 클라이언트 -> 백엔드 -> Pre-signed URL 발급(제한 시간만큼만 유효) -> 클라이언트 -> 직접 S3 업로드
      - 서버 부하 줄이고 네트워크 비용 절감
- 단일 업로드
  - 8MB 이하
    - Pre-signed URL 방식
- 멀티파트 업로드
  - 8MB 초과
  - 큰 파일을 8Mb 파트로 분할하여 개별 업로드
    - 신뢰성
    - 병렬 업로드
    - 진행률 추적
  - 클라이언트 -> 백엔드 -> 오브젝트 스토리지(S3) -> uploadId 반환
    - uploadId + Pre-signed URL (8MB)
      - ETag 반환 -> 
---

# 42. 미디어 업로드 ▶︎ 구현과 동작

---

# 43. 미디어 조회 흐름 (보안과 CDN)
- CDN 사용 -> 자주 조회 이미지가 캐싱되어 빠르게 조회 가능
  - 가까운 데이터 센터 CDN
  - S3 조회가 들어든다.
  - 미디어 서비스에서 반드시 써야 한다.
  - 고정 URL 필요 (presignedURL은 매번 달라져서 캐싱 불가)
    - Cache-Aside
- CDN 종류
  - Cloudflare
  - AWS CloudFront
  - 등등등

---

# 44. 미디어 조회 흐름 ▶︎ 구현과 동작


---

# 45. 미디어:게시글 관계 (N:1)
- 비동기 미디어 처리
- 업로드 완료
  - MQ
    - 미디어 워커 (consumer)
      - 썸네일 -> DB
      - 트랜스코딩 -> DB
      - 메타데이터 -> DB

---

# 46. 미디어:게시글 관계 ▶︎ 구현과 동작

---

# 47. [AI 다루기] 미디어 서비스
- claude code
  - 명령어
    - 미디어 도메인을 구현해줘. 미디어 엔티티는 다음과 같이 구성해줘. --- Long id, MediaType mediaType // IMAGE, VIDEO, String path, Status status // INPUT, UPLOADED, COMPLATED, FAILED, Long userId, String uploadId // 멀티파트 업로드 ID, JSON attributes // 확장 메타데이터 --- 그리고 오브젝트 스토리지로 rustfs 를 사용할거야. rustfs:1.0.0-alpha.72 이 이미지를 사용하면 돼. --- 미디어 업로드를 하기 위해서는 presigned-url 을 받아와야 하는데, POST /api/v1/media/init (fileSize <= 8MB) 이 API 응답으로 받아올 수 있어. 그 후 클라이언트는 미디어가 8MB 이하면 해당 presigned-url 로 단일업로드를 하고, 초과하면 8MB 단위로 미디어를 분할해서 여러 presigned-url로 업로드하면 돼. 그래서 받은 etag 로 완료 API를 호출하면 돼. 완료 API는 POST /api/v1/media/uploaded 이거를 사용하면 돼. 그러면 서버에서는 media 에 해당 상태 등을 업데이트 해주면 돼.
    - 가능한 부분은 병렬로 빠르게 처리해줘.
    - /compact
    - rustfs 에 presigned-url 받아서 멀티파트 업로드하는 부분 재시도 해봐. pathStyleAccessEnabled 이 부분도 함께 고려해줘.
    - midia/midia/10 이런식으로 만들어지고 있는데, media/1 이렇게 되도록 해줘.
---

# 미디어 서비스 직접 구현해보기

---
