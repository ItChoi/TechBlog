# 10. 사용자 회원가입
- PasswordEncoder -> Argon2PasswordEncoder.defaultsForSpringSecurity_v5_8();
  - 2015 패스워드 해싱 컴패티션에서 우승한 알고리즘
---

# 11. 사용자 회원가입 ▶︎ 구현과 동작

---

# 12. 사용자 로그인
- 관리자 콘솔 -> api 호출 타임라인 보려고 할 때, 사라지는 경우 'preserve log' 체크 후 진행 
---

# 13. 사용자 로그인 ▶︎ 구현과 동작

---

# 14. 세션 로그인 (프레임워크)

---

# 15. 세션 로그인 ▶︎ 구현과 동작

---

# 16. 공유 세션 (Redis)

---

# 17. 공유 세션 ▶︎ 구현과 동작

---

# 18. [AI 다루기] 사용자 도메인, 회원가입
- claude code
  - User 도메인을 만들고 필드는 id, username, password를 가지게 해줘. --- 패스워드 인코딩은 Argon2 사용해줘. --- 회원 가입 API를 만들어줘. API 규칙은 /api/v1/users/signup 과 같이 해줘.
  - 가능한건 병렬로 처리해줘.
  - SQL 로그 남기게해줘.
  - SQL 문 한 줄로 로깅하게 해줘. 바인딩 된 파라미터는 안 보여도 돼.
  - logging 레벨과 format_sql은 기본값 그대로 사용하고 싶어. 필요 없는지 확인해보고 지워.

---

# 19. [AI 다루기] 사용자 인증
- claude code
  - /context
  - /clear
    - 새로운 주제, 앞에 내용이 불필요한 경우 사용 
  - Spring Security 를 사용해서 로그인을 구현해줘. 기본 인메모리 세션을 사용하도록 구성해줘.
  - Spring Security를 Form 로그인을 사용해서 최대한 Spring Security를 사용한 로그인을 구현해줘. 기본 인메모리 세션을 사용하도록 구성해줘.
  - 직접 login API를 만들지마. 관련 코드는 제거하고, Spring Security가 제공하는 login 에서 URL만 "/api/v1/login" 이렇게 바꿔주도록 구성해줘.
  - 아래 코드가 필요없다고 판단되면 제거해줘. --- code --- 그리고, successHandler, failureHandler는 너무 기니까, 다른 파일로 빼줘 
---

# 20. [AI 다루기] 사용자 인증 공유 세션
- llm -> 특정 시점 학습 데이터 반영 -> 업데이트 하려면 context7 플러그인 설치
  - claude code
    - /plugin
      - discover -> context7 설치
- claude code
  - Redis를 사용해서 공용 세션 저장소를 만들어주고, 현재 애플리케이션에 연동해줘.
    - 훈련 shift + tab -> plan mode on 엔터
      - 역으로 질문 한다.
  - 여기에 세션 관리 정책도 관리할 수 있는 구성을 하게 해줘. 예를들어 아래 기능이 필요해 --- .sessionManagement(session -> session
    .maximumSessions(2)
    .maxSessionsPreventsLogin(false)
    .sessionRegistry(sessionRegistry)
    ) ---
- /context 가 가득 차지 않게 유지하는게 좋지만, 가득 차면 /compact 를 통해 요약해서 요약본을 새로운 context에 등록하면 된다.
- 
---

# 미션 사용자, 인증 서비스 직접 구현해보기
- 요구사항
  - 사용자 서비스를 직접 구현해봅시다.
  1. 사용자 도메인, 테이블 설계
  2. 회원가입
  3. 로그인 (세션 로그인)
  4. 공용 세션 로그인
  5. 로그아웃
  - User DB 는 인메모리 H2 로 구현합니다. (팔로우 서비스에서 RDB 를 사용할 때 같이 연동해봅니다.)
  - 로그인 구현시 Spring Security 를 활용해봅니다.
  - 공용 세션으로 Redis 를 사용해봅니다.

---
